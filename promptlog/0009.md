# Prompt 0009

**Working Directory:** .

**Prompt:**

Why might I want different communities for different services on the same box? Could I then satisfy that with a configuration file that I would load with per-task customization? The file would look something like: 

config:
  peers:
    - ip: 10.0.1.1
      as: 65000
  origin: igp
  as: 65000
  communities:
   - "65000:2" # I'm not sure what communities I'd want normally
per-ip:
  10.0.0.1/32:
    as: 65000
    communities:
     - "65000:2" # I'm not sure what communities I'd want normally

What else am I missing for this config file? The idea would be to put a config block in normally, and not anything under per-ip normally.
