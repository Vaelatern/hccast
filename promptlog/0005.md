# Prompt 0005

**Working Directory:** .

**Prompt:**

In Go how can we inspect an interface (on Linux) to see the `alias` field? It's a comment text box that is bounded to 255 printed characters (256 includes a null byte). I'll be doing things like "sudo ip link set dev vethh-adfa8380 alias "healthcheck: ok"" and wanting to filter down interfaces based on the existance of the healthcheck:ok text
